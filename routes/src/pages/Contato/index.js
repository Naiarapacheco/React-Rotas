import { Link } from 'react-router-dom';

function Contato(){
    return(
        <div>
            <h1>Bem vindo a página de Contato</h1>
            <br/>
        </div>
    );
}

export default Contato;